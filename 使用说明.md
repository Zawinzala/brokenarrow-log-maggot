# 断箭蛆工具 byZola（Electron 版）

基于游戏官方 GameLogs 日志 + [BATrace](https://app.batrace.top/) 公开数据查询的本地辅助工具。
替代旧的「断箭蛆工具」（旧工具因 BATrace API 改版已失效，本版本已适配新版接口）。

## 功能

- **日志实时监听**：监听你设置的 GameLogs 目录，自动识别当前对局（地图 / 对局 ID / 玩家名单 / 队伍 / 卡组）。除原生 `Gamelog__*.log` 外，也兼容用户自放的 `.log/.txt` 文本日志（同名/多文件时原生日志优先）。
- **房间内玩家**：开战前就能看到房内玩家名单（日志 Incoming client 行，ID 即 BATrace ID），点「刷新粗查」即可提前查看情报。
- **粗查（多人）**：检测到对局后自动查询每位参战玩家的 ELO、K/D、胜率、样本数、偏好兵种、最爱单位（**开战后自动查询，优先查敌人那队**）。每名玩家只查 1 次，10v10 一局约 10 次调用（上限 20 次，机器人/观战/重复自动跳过），顶栏实时显示「API 已用 X / 120（24h）」。每个玩家卡片带 📋 复制（一行情报），每队标题带「复制单队」（名字+ELO 一行），方便报备。
- **蛆查（单人）**：只有单人查询触发，算法与「断箭蛆指数」网站 V4 完全同步——拉取最近 12 场带 ELO 变动的有效对局，按每局在己方队伍内的 MVP 名次求平均，经余弦平滑曲线映射到 1.0~10.0。附 12 局明细表、KD/击杀/MVP/经济排名、胜率、变强/变蛆趋势。
- **玩家粗查报告**：一次调用生成 ELO、K/D、DMR、胜率、偏好兵种、最爱单位、地图表现、打法风格、近期对局表，支持复制单行情报。
- **玩家搜索**：按名字或 ID 搜索 BATrace 玩家。
- **房间内谁也在用本工具**：进房/对局后，房间玩家卡片上带 **🎮** 的表示对方也在使用本工具（服务端仅在你自己的房间玩家 ID 里比对、只返回匹配项，且要求你本人是活跃工具用户，不暴露其他玩家数据）。
- **对局档案**：本地 matches 表自动保存最近 500 局，表格显示 **状态(胜/负/观战/未知)｜模式(排位/自定义，彩色标签)｜地图｜ELO变化(+XX.XX/结算ELO，两位小数)｜时间｜[账号]**（时间倒序），右键行可「🔄 刷新对局信息」（从 API 重新拉取并回写、预览同步刷新）。左键点开详情：地图/ID/模式/结果/ELO变化/结算ELO/本机评分/本机账号/时间/时长 + 胜方/败方/观战三组玩家表格（名字/ID/队伍/ELO变化/评分/占点/击杀/伤害/承伤/K-D/补给/经验/奖牌 全列展示）；加载中显示雷达扫描动画（真请求才显示，缓存命中秒开）；本地缺数据时自动从 API 补齐并回写本地、首页预览同步刷新（点击触发、24h 缓存）。不提供清空。
- **APM 监测功能（真实输入）**：统计**真实鼠标点击 + 键盘按键**（仅当游戏窗口在前台时计数），对局结束生成每分钟操作图。默认**关闭**，需在「设置」中开启「APM 监测功能」（未开启时 APM 栏目自动收起）。输入钩子不可用 / 未开启 / 回放日志时，卡片直接提示「不可用 / 无法打开」，不会显示任何伪造的参考数据。
- **卡组工具**：两列同栏布局——前线卡组 / 后勤仓库。备份 / 恢复游戏卡组（前线 Decks/*.dek ⇄ 后勤 DeckBackups/*.zip），支持「备份全部」「备份选中」、一键部署、删除、打开目录（默认按修改时间倒序排列）。卡组目录自动检测（兼容不同机器上的目录名/位置），找不到时明确提示而非静默失败。每局游戏开始会自动把当前前线卡组打包成唯一的「上一局卡组包.zip」（放后勤仓库，下一局开始自动覆盖），防止同步/部署覆盖前丢失当前卡组。
- **玩家追踪（仿 VRCX）**：工具持续记录见过的玩家（ID / 名字 / 改名史 / 相遇对局），数据存本地 `players-db.json`。追踪库以「对局」为中心，对局 ID 为唯一键，只保存：对局 ID、地图名、对局时间、时长（有则存）、胜方、玩家名字列表、玩家数字 ID 列表；**没有对局 ID 的对局不纳入统计**。房间/大厅/搜索/报告里见过的人也会更新名字与最近出现（不计入相遇次数）。
- **右键「调查」**：当前房间/对局/上一局的玩家卡片、搜索候选、报告与蛆指数页头的玩家名，右键都有「🔍 调查羁绊」：看到**最新 ELO**（来自 `/api/players/matches`，取最近一场带评分的局，自定义局无评分自动向前推一局；全无则提示暂无排位数据）、**最近 10 局**（时间/地图/ELO 变化/胜或负/自定义）、相遇次数、队友/敌人次数与胜负（胜/负/未知/自定义）、相遇记录表（每行含对局 ID，标注「排位/自定义」，观战用独立配色，右键跳 BATrace 对局；自定义局也能显示输赢）、改名历史、是否被封，以及胜率/偏好/最爱情报（6 小时缓存，可刷新）。「最近 10 局」与「相遇记录」双栏并排，最新 ELO 与胜率/偏好/最爱情报合并在一处显示；读取到真实 API 时整框显示雷达加载动画（纯视觉、无交互；数据返回即切换内容，缓存命中则秒开不显示）。点对局/房间玩家卡片加载报告后会自动滚动到报告区。
- **我的对局同步（每小时）**：拉取 `/api/players/matches?stbid=<本机ID>&limit=10`，按对局 ID upsert 回填地图名/时长/胜负/ELO/模式/评分。胜负判定三级链：① `WinnerTeam`(0=Alpha胜/1=Bravo胜) 对比本机队伍 → ② 排位用本机 `NewRating-OldRating` 正负 → ③ 自定义无 WT 用 `analysis/match` 的 `destructionScore`（摧毁分）高者胜（**占领分不可靠，已废弃**）→ 都无则「未知」。**观战局（本机 TeamId=100）状态恒为「观战」**，不计胜负。`players/matches` 队伍编码：`TeamId` 缺失=队伍A、`1`=队伍B、`100`=观战；模式：`Type` 无=排位、有(1/3)=自定义。日志即时记录（只收带对局 ID 的局，打完立刻入库，重放不重复）。本机 ID 从对局名单自动检测，未打对局前跳过。
- **封禁监控（每小时）**：拉取 `/api/leaderboard/ban?limit=500`，按 stbid 与本地库 diff，**只对「之前遇到过」的新被封玩家弹系统提醒**，其余新封静默入库；「🛡 封禁追踪」卡片默认隐藏（设置里勾选「显示封禁追踪栏目」才显示），列出名字/ID/ELO/首次发现/遇到过标记。
- **主题**：设置弹窗内可切换 黑 / 白 / 青 / 橙 / 紫夜 / 深林 / 深海 七种主题（白/青/橙为亮色底，紫夜/深林/深海为精修暗色系），选择即预览，保存后持久化。
- **开发者测试（设置弹窗底部）**：仅开发自测用、不影响正常数据——「测试封禁提醒（遇到过）」模拟弹一条你遇到过的玩家被封提醒（不改封禁库）；「立即同步我的对局」「立即检查封禁」手动触发真实的每小时同步（幂等、按对局 ID 去重）。
- **上一局**：当前房间卡片右上「🕘 上一局」可查看最近一场已完成对局的名单（按队伍分组、卡片可点开报告），打开即自动粗查一次（同一局去重）；点「刷新粗查」会**重新查询上一局名单**（保持上一局视图，不跳回当前）；再点按钮或新的对局/房间事件会自动回到当前视图。

- **对局录像（卡片常驻显示；自动录制开关在「对局录像」卡片右上角，iOS 风格——关掉只停止自动录制，模块照常可查/可播）**：每局游戏开始会自动录制**你自己的屏幕视角**（每 1 秒截一帧游戏画面，合成 1fps 的 WebM，**1080p、8000kbps**，本地纯保存、无云端）——2K/4K 屏幕都支持，一局约 20-80MB（视时长与画面复杂度）。局结束自动保存到本地 + 系统通知（无「保存并上传」确认，不强制上传）。App 内「📹 对局录像」卡片按对局 ID 搜索，列表按录像时间倒序（最新在最前，测试录像负数 ID 不参与排序）；录制进行中卡片内置**小预览**（每秒刷新，显示正在录制的屏幕源，确认没录错屏）。录像行可右键 → 打开本地位置 / 打开对局详情 / 在 BATrace 打开对局 / 删除录像；「设置 → 本地录像管理」：删除 30 天前 / 删除全部 / 「📂 打开本地录像位置」（只动本地文件）；单条删除在录像卡片列表的 🗑 按钮。若发现没录到东西，可用「设置 → 开发者测试 → 🎥 录制测试（1 分钟·只存本地）」自测，结果会存到本地；录制过程会写入 `%APPDATA%/broken-arrow-log-assistant/replay.log` 便于排查。有本地录像的对局会在「对局档案」里标 📹，**点 📹 直接播放该局录像**；同一局有多个本地文件会先弹列表让你选择。播放器支持 1x/2x/3x 倍速（1 秒/帧：1x=实时、3x=3 倍速）。录像=屏幕录制，只含你自己看到的画面，不含他人视角。
- **版本更新 / 公告（Cloudflare）**：启动时从 `brokenarrow.zolahere.top/update-meta` 拉取公告（**每次启动弹一次**）与最新版本；有新版本时顶栏横幅出现「⬇ 下载新版本」，直接从 Cloudflare（R2 自定义域）下载新版 exe。发布与管理：打开仓库里 `admin/update-console.html`（本地管理站，需与同目录 `world-map-data.js` 一起使用）——拖入新版本 exe 上传、填版本号 / 更新内容 / 公告后发布；同一页面还能查看心跳统计（`/users`：当前在线、历史、累计心跳、版本分布、**世界地图光点**、国家/地区分布等），并可按 **1小时 / 1天 / 1周 / 1月 / 全部** 切换统计范围。
- **API 稳定性灯（顶栏 ●）**：每小时用 4 个轻量小接口探测 BATrace 稳定性（不计入 240 配额）：全通=绿、部分=黄、全挂=红、尚未检测=灰；鼠标悬停显示各端点明细与检测时间，点击圆点可立即重测。
- **离线本地兜底**：本地会保存「上次已知 ELO/胜率/偏好」快照；API 不可用时，玩家卡片显示「本地 ELO（离线）」、「调查羁绊」情报区显示本地快照、搜索回退为「本地见过的玩家」匹配（会标注离线）。
- **软件更新提醒**：启动时读取 [version.txt](https://github.com/Zawinzala/brokenarrow-log-maggot/blob/main/version.txt)（第一行为版本号，如 `v3.1.0`；其余行为公告），有新版本时顶部横幅提醒并显示公告。版本检查同样带「直连失败→免费代理兜底」，国内网络也能收到更新提醒。
- **关于与致谢**：内置软件原理声明（只读公开日志与公开接口、不碰内存/注入/反作弊），并附 BATrace 查询站与蛆指数网站的致谢链接。
- **24 小时 API 配额**：软件内置每 24 小时最多 240 次真实请求的保护（缓存命中不计入），防止刷爆查询站；超限后自动暂停查询，次日窗口滚动恢复。该上限固定，不向用户开放修改。
- **请求间隔（bypass）**：默认 1.2 秒/次（约 1 次/秒，适配 BATrace 限流）。作者可通过 Cloudflare 环境变量 `BATRACE_BYPASS_UA` 向所有客户端下发专属请求头，收到后自动提速到 300ms/次；未收到则维持 1.2 秒。启用后「设置 → 开发者测试」区会以绿色小字显示「⚡ 开发者：BATrace 专属提速已启用」。顶栏的「BATrace ●」灯 = 每小时一次的 API 稳定性检测（轻量小接口，不计入 240 配额；点击可立即重测），旁边「● X 人正在使用」是心跳在线人数（绿灯=连接成功、红灯=连接失败）。
- **使用统计**：默认开启心跳（每 10 分钟一次，服务端已做写入节流适配免费配额），让作者看到在线人数（只上报匿名 ID + 版本号，不含任何玩家数据；可在「设置」里关闭）。心跳使用自定义域名直连，不走代理。

> 监听轮询间隔、API 请求间隔、统计服务地址、对象存储配置均为**内部固定值**，不在设置界面暴露（防止误改）。

## 关于 APM 与反作弊

- APM 统计使用 Windows 全局**只读**输入钩子（低级键盘/鼠标钩子），只读取、不拦截、不模拟、不注入任何输入，属于“按键记录类”接口，本身不是作弊软件。
- 但《断箭》1.0.11 起已接入 Easy Anti-Cheat（EAC），全局输入钩子历来是反作弊重点观察对象，**风险并非为零**。因此本工具默认**关闭**该功能，由你自己决定是否开启；不开启也完全不影响其他功能。
- 若担心封号风险，请保持关闭；开启后如遇任何账号争议，请自行评估与承担。该功能可随时在「设置」中关闭，关闭后全局钩子会立即停止。

## 环境要求

- Windows + [Node.js](https://nodejs.org/)（建议 18+）
- 游戏《断箭》（Broken Arrow），日志目录形如：
  `C:\Program Files (x86)\Steam\steamapps\common\broken_arrow\GameLogs`

## 安装与运行

```powershell
# 首次：安装 Electron（需要联网，装一次即可）
npm install

# 启动
npm start
```

启动后在「设置」里选择日志目录：
- 点「自动检测 Steam 目录」会自动找默认安装路径；
- 也可以手动点「浏览…」选择；
- 没有装游戏时，可以把任意 `Gamelog__*.log` 复制到一个文件夹，把目录指过去也能测试。

## 粗查与蛆查

- **粗查**：点搜索结果 = 1 次 API 调用，快速看 ELO/胜率/偏好/最爱单位。
- **蛆查**：点「🐛 查蛆指数」按钮。冷查需要拉取最近 12 场对局明细，约 13~14 次调用（单局明细 24 小时缓存、分析 6 小时缓存，同一天重复蛆查几乎不消耗调用）。
- 蛆指数只在单人蛆查里出现，多人粗查不涉及。

## 打包为 EXE（发布版）

本工程已内置 [electron-builder](https://www.electron.build/) 配置（`package.json` 的 `build` 字段）。

```powershell
# 1) 安装打包工具（联网，一次即可）
npm install

# 2) 打出安装版 + 便携版（dist/ 下）
npm run dist
```

- 产物在 `dist/`：`断箭蛆工具 byZola Setup x.y.z.exe`（安装版）与 `断箭蛆工具 byZola x.y.z.exe`（便携版，双击即用）。
- 未提供自定义图标时使用 Electron 默认图标；想换图标，把 `build/icon.ico` 放到工程根目录（electron-builder 会自动识别）。
- 版本号改 `package.json` 的 `version` 字段即可（会同时显示在标题栏与「关于」里）。

### GitHub 自动打包（推荐发布方式）

仓库已内置 `.github/workflows/build.yml`，推送版本标签即可自动打包并发布：

```powershell
git add -A
git commit -m "v3.0.0"
git push
git tag v3.0.0
git push origin v3.0.0
```

- Actions 会在 Windows 上自动 `npm install && npm run dist`，把 `dist/*.exe` 上传为构建产物，并（仅打标签时）自动创建一个 GitHub Release 附带安装版/便携版 exe，用户可直接下载。
- 也可以不推标签，在仓库 Actions 页面手动点「Run workflow」只出产物不出 Release。
- 首次使用前确认：本地已跑过一次 `npm install` 生成最新 `package-lock.json`（本工程已把 electron-builder 加进依赖，锁文件需刷新一次再推送）。

## 版本提醒机制

- 工具每次启动会访问 `https://raw.githubusercontent.com/Zawinzala/brokenarrow-log-maggot/main/version.txt`。
- 文件格式：第一行版本号（如 `v3.1.0`），后续行是公告（可空）：
  ```
  v3.1.0
  公告：修复了蛆查，新增房间名单
  ```
- 当远端版本号高于本机 `package.json` 版本时，顶部弹横幅 + 公告，并链接到 GitHub 页面。

## 心跳统计（作者用来统计在线人数）

- **固定开启、不可关闭**（为统计在线人数与房间工具用户；不会关闭），每 1 分钟上报一次：**匿名 ID + 版本号 + 游戏内用户名 + 游戏数字 ID**（IP 由服务端自动从请求头获取，客户端不上报 IP；不含对局/战绩数据），顶栏显示当前在线人数。
- 默认统计服务：`https://brokenarrow.zolahere.top`（内部固定，不在设置里暴露）。
- 客户端接口约定：`POST {url}/heartbeat`（body `{ userId, v, name, uid }`）+ `GET {url}/online-count`。参考实现见 `server/worker.js`（Cloudflare Workers，一个文件）。
- 上报失败静默忽略，不影响软件使用；「设置 → 测试心跳」可手动触发一次并显示上报结果，便于排查服务器问题。
- **开发者查询接口**（一个就够）：`GET {url}/users?token=…`，token = Worker 环境变量 `HEARTBEAT_DEV_TOKEN`（在 Cloudflare Workers「设置 → 变量和机密」配置，不写死在仓库）。一次返回 `{ ok, onlineCount, online:[…], historyCount, history:[…] }`：`online` 是当前在线明细（名字/游戏ID/匿名ID/IP/版本/最后心跳/地理位置），`history` 是历史所有连接过的人（含首次/最后时间与上报次数；存 KV `hist:` 前缀、不设 TTL，一直累计）。服务端在每次心跳时用 Cloudflare 的 `request.cf` 自动采集连接 IP 的国家/地区/城市/经纬度（存入每条记录的 `geo` 字段），管理站据此在世界地图上打光点；**注意：经代理兜底上报的请求，其地理位置会显示为代理服务器所在位置，POST 直连上报才是真实位置**。
- 心跳已使用自定义域名（`brokenarrow.zolahere.top`）直连，**全链路不再走任何免费代理**（版本检查/bypass 拉取也直连）；服务不可达时直接显示红灯/报错，方便你第一时间发现服务故障。

## 技术要点

- **CORS 与 Electron**：BATrace 接口不带 `Access-Control-Allow-Origin`，浏览器页面无法跨域调用（所以之前只能写油猴脚本）。Electron 主进程是 Node 环境，直接发 HTTP 请求不受 CORS 限制——所有请求都走主进程，界面只通过 IPC 拿数据。
- **日志读取**：只增量读取最新日志文件的新增字节（历史日志可能几百 MB，绝不整读）；游戏重启生成新日志时自动切换文件。文件匹配放宽为 `.log/.txt`（方便自放测试日志），多个文件时 `Gamelog__` 前缀优先、再按修改时间取最新。
- **核心接口（2026-08 实测有效）**：
  - `GET /api/analysis/player?stbid=` 一次返回 ELO 趋势 / 胜负 / 最爱单位 / 偏好兵种 / 地图表现 / 打法——粗查与报告都走它，6 小时磁盘缓存
  - `GET /api/analysis/match?matchid=` 单局完整数据（`mvpRanking` 队内 MVP 得分 + `economy`）——蛆查专用，24 小时磁盘缓存
  - `GET /api/players/search?q=&limit=` 玩家搜索（10 分钟缓存）
- **限流与缓存**：请求默认间隔 1200ms（约 1 次/秒，适配 BATrace 限流；服务端下发专属 bypass 时自动提速到 300ms），结果写本地磁盘缓存。顶栏「API 已用 X / 240（24h）」统计所有真正打到 BATrace 的请求数（缓存命中不计；后台同步/补胜负也计入），并受内置的 24 小时 240 次配额保护（固定值，不向用户开放修改）。
- **蛆指数同步**：算法、等级文案（👑神/🦁团队支柱/😐平平淡淡/🐛有点蛆/💩蛆！）、取数口径（近 12 场带 ELO 变动的有效对局、队内 MVP 名次、余弦曲线）与网站 V4 完全一致，核心代码在 `src/analyzer.js` 的 `buildMaggotReport`。

## 已知限制

- 老对局（2026-08 之前的部分对局）在 BATrace 中缺少部分字段，胜负显示为「未知」。
- 有效对局不足 12 场（或对局明细少于 10 人）时无法计算蛆指数，会给出提示。
- 地图名优先从 `mapPerformance` 动态注册（`src/analyzer.js`），只有从未出现过的地图才显示 `地图#N`。
- 只读日志 + 公开接口，不碰内存、不注入、不影响反作弊。

## 目录结构

```
main.js                 Electron 主进程（窗口 / IPC / 版本检查 / 蛆查装配）
preload.js              渲染进程安全桥接
src/config.js           设置读写 + Steam 目录自动检测
src/logParser.js        日志解析状态机（对局 / 房间名单 / 卡组）
src/logWatcher.js       目录轮询 + 增量读取
src/batrace.js          BATrace API 客户端（限流 + 缓存 + 24h 配额计数）
src/heartbeat.js        心跳统计（每 10 分钟一次，匿名上报在线；直连，不走代理）
src/apiHealth.js        API 稳定性灯（每小时轻量探测，不计配额）
src/analyzer.js         玩家分析（粗查报告 / 蛆查同步网站算法）
src/storage.js          本地对局档案
src/replayRecorder.js   对局录像录制（截屏合成 WebM，隐藏窗口抓屏；1080p/1s/8Mbps，纯本地）
src/s3Client.js         S3 兼容对象存储客户端（SigV4 预签名 + 列表 + 文件名编码）
renderer/               界面（原生 HTML/CSS/JS）
server/                 心跳统计服务端示例（Cloudflare Workers）
.github/workflows/      GitHub Actions 自动打包（推 v* 标签即发布）
```
