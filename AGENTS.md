# AGENTS.md — 断箭蛆工具开发须知

## 最重要的约束：API 调用必须省着用

本工具依赖外部查询站 BATrace 的公开接口。该站不稳定，且我们不想给它造成负担。**任何时候都要把 API 调用数量降到最低**，不要让用户随便改大调用频率。

具体规则（必须遵守）：
- **API 稳定性灯（BATrace ●）只允许每小时自动检测一次**：不允许提供「点击重测 / 立即检测」等任何用户可手动触发探测的入口（点一次就是 4 个接口请求）。当前已按要求移除点击重测与「立即检测」按钮。
- 24 小时配额固定为 **240 次**，不允许在设置里暴露修改入口，也不允许在代码里放开。
- 后台同步（封禁榜 / 本机对局 / 胜负回填）按每小时一次的节奏，不要加频。
- 缓存优先：能命中缓存就用缓存，不要绕过缓存重复拉同一接口；只有需要保证「每小时真拉取」的后台同步（封禁/对局）才用短 TTL。
- 新增任何会请求 BATrace 的功能前，先想清楚它会不会增加调用量；要加的话，优先复用已有缓存/本地数据（players-db、batrace-cache、api-health.json）。
- 健康检查探针不计入 240 配额，但同样只允许每小时一次，不得加频。

一句话：**面向 BATrace 的请求能少则少，别给用户留任何「手痒多按一下」的入口。**
