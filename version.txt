v3.5.0
公告：v3.5.0 ——
• BATrace 请求限流适配：默认间隔 1.2 秒/次（避开 Eero 1 req/s 限流）；服务端下发专属 bypass（User-Agent）时自动提速到 300ms/次
• 对局录像：移除云端上传/共享，改为纯本地保存（不怕存储桶到期）；本地录制升级为 1080p、1 秒 1 帧、8000kbps，看得更清楚
• 录像播放：1x=实时、2x/3x 倍速，进度条正常
• 心跳：间隔 10 分钟一次 + 服务端写入节流，适配免费 KV 配额